components:
  parameters:
    chat-id:
      name: chat-id
      in: path
      required: true
      schema:
        type: string
        format: uuid
        description: The ID of the chat.
    token:
      name: token
      in: query
      required: true
      schema:
        type: string
        description: The token of the user.